import React from "react";
import Root from '../../style/Root';

import WritingEntryButton from "@/src/components/buttons/WritingEntryButton";

const GrowCheckHome: React.FC = () => {
  return (
    <Root>
      <div>GrowCheckHome</div>
      <WritingEntryButton/>
    </Root>
  );
};

export default GrowCheckHome;
